services:
  - type: web
    name: asas-extractor
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false   # Set this manually in the Render dashboard
